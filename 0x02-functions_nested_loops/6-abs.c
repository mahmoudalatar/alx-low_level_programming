#include "main.h"
#include <stdlib.h>

/**
 * _abs -  use to computes the absolute value of integer
 * @n: the number will be use to compute
 * Return: the resualt form _abs as int
 */

int _abs(int n)
{
	return (abs(n));
}
