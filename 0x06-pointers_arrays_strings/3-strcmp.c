#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first string will be compared
 * @s2: second string will be compared
 * Return: 1 if the s1 bigger 0 if equal -1 if smaller than s2
 */

int _strcmp(char *s1, char *s2)
{
	int result = strcmp(s1, s2);

	return (result);
}
