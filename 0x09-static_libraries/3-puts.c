#include "main.h"
#include <stdio.h>
/**
 * _puts - printing
 * @str: string
 * Return: nothing
 */

void _puts(char *str);

/**
 * main - use as Entry point
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
